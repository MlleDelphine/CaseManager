{% extends 'base.html.twig' %}

{% block breadcrumb %}
    <li class="">{{ "customers_management"|trans|capitalize }}</li>
    <li class="active">{{ "all_customer_contacts"|trans|capitalize|capitalize }}</li>
{% endblock %}

{%  block page_title %} {{ "customer_contacts"|trans|capitalize}} {% endblock %}
{% block title %} {{ "customer_contacts"|trans|capitalize }} {% endblock %}

{% block body %}
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><i class="fa fa-address-book"></i> {{ "all_customer_contacts"|trans|capitalize }}</h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li>
                        <button id="import-btn" type="button" class="btn btn-primary" data-toggle="modal" data-target="#importModal">
                            <i class="fa fa-upload" aria-hidden="true"></i> {{ "import_verb"|trans|capitalize }}
                        </button>
                    </li>
                    <li>
                        <a type="button" class="btn btn-warning btn-li" href="{{ path('customer_contact_export_all') }}"><i class="fa fa-download" aria-hidden="true"></i> {{ "export_all"|trans|capitalize }}</a>
                    </li>
                    <li class="inherit-li">
                        <a class="btn btn-success btn-li" href="{{ path("customer_contact_new") }}"><i class="fa fa-user-plus"></i> {{ "addition"|trans|capitalize }}</a>
                    </li>
                    <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                    </li>
                    <li><a class="close-link"><i class="fa fa-close"></i></a>
                    </li>
                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content container">
                <a href="#" class="btn btn-default openall"><i class="fa fa-angle-down" aria-hidden="true"></i> {{ "unfold"|trans|capitalize }}</a>
                <a href="#" class="btn btn-default closeall"><i class="fa fa-angle-up" aria-hidden="true"></i> {{ "fold"|trans|capitalize }}</a>
                <hr>
                <!-- start accordion -->
                <div class="panel-group accordion" id="accordion" role="tablist" aria-multiselectable="true">
                    {% for customerContact in customerContacts %}
                        <div class="panel">
                            <a class="panel-heading" role="tab" id="heading{{ customerContact.id }}" data-toggle="collapse" data-parent="#accordion" href="#collapse{{ customerContact.id }}" aria-expanded="true" aria-controls="collapse{{ customerContact.id }}">
                                <h4 class="panel-title">{{ customerContact.firstName ~ ' ' ~ customerContact.lastName }}</h4>
                            </a>
                            <div id="collapse{{ customerContact.id }}" class="panel-collapse collapse{% if loop.first %} in{% endif %}" role="tabpanel" aria-labelledby="heading{{ customerContact.id }}">
                                <div class="panel-body">
                                    <table class="table table-bordered">
                                        <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>{{ "honorific"|trans|capitalize }}</th>
                                            <th>{{ "firstname"|trans|capitalize }}</th>
                                            <th>{{ "lastname"|trans|capitalize }}</th>
                                            <th>{{ "mail_address"|trans|capitalize }}</th>
                                            <th>{{ "corporation_jobstatus"|trans|capitalize }}</th>
                                            <th>{{ "corporation_site"|trans|capitalize }}</th>
                                            <th>{{ "created_m_s"|trans|capitalize }} {{ "on_date"|trans }}</th>
                                            <th>{{ "updated_m_s"|trans|capitalize }} {{ "on_date"|trans }}</th>
                                            <th style="text-align: center;">{{ "actions"|trans|capitalize }}</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        <tr>
                                            <th scope="row">{{ customerContact.id }}</th>
                                            <td>{{ customerContact.honorific|trans|capitalize }}</td>
                                            <td>{{ customerContact.firstName }}</td>
                                            <td>{{ customerContact.lastName }}</td>
                                            <td>{{ customerContact.mailAddress }}</td>
                                            <td>{{ customerContact.corporationJobstatus.name }}</td>
                                            <td>{{ customerContact.corporationSite.name }}</td>
                                            <td>{% if customerContact.created %}{{ customerContact.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td>{% if customerContact.updated %}{{ customerContact.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td align="center" style="vertical-align: middle">
                                                <a class="btn btn-sm btn-info" href="{{ path('customer_contact_edit', { 'slug': customerContact.slug }) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i>  {{ "modify"|trans|capitalize }}</a>
                                                <a class="btn btn-sm btn-warning" href="{{ path('customer_contact_export', { 'slug': customerContact.slug }) }}"><i class="fa fa-download" aria-hidden="true"></i> {{ "export_verb"|trans|capitalize }}</a>
                                                <button id="import-btn" type="button" class="btn btn-sm btn-danger delete-entity" data-toggle="modal" data-target="#deleteModal" data-slug="{{ customerContact.slug }}">
                                                    <i class="fa fa-trash" aria-hidden="true"></i> {{ "delete_verb"|trans|capitalize }}
                                                </button>
                                            </td>
                                        </tr>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <!-- end of accordion -->
            </div>
        </div>
    </div>

    <!-- Import Modal -->
    {% include  ":common:import_modal.html.twig" with { "action_path": "customer_contact_index", "object_title": "some_customer_contacts", "error": error } %}
    <!-- Delete Modal -->
    {% include  ":common:remove_object_modal.html.twig" with { "default": true } %}
{% endblock %}

{% block custom_javascripts %}
    {{ parent() }}
    {% if error %}
        $(document).ready(function(){
        $("#import-btn").trigger("click");
        });
    {% endif %}

    {% include ":common:delete_pop_up.js.twig" with { "routeName": "customer_contact_delete" } %}
{% endblock %}