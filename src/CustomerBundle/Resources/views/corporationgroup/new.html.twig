{% extends 'base.html.twig' %}

{% block body %}
    <h1>Corporationgroup creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('corporation_group_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
