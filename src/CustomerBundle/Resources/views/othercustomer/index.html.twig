{% extends 'base.html.twig' %}

{% block breadcrumb %}
    <li class="">{{ "customers_management"|trans|capitalize }}</li>
    <li class="active">{{ "all_other_customers"|trans|capitalize|capitalize }}</li>
{% endblock %}

{%  block page_title %} {{ "other_customers"|trans|capitalize}} {% endblock %}
{% block title %} {{ "other_customers"|trans|capitalize }} {% endblock %}

{% block body %}
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2><i class="fa fa-share-alt-square"></i> {{ "all_other_customers"|trans|capitalize }}</h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li>
                        <button id="import-btn" type="button" class="btn btn-primary" data-toggle="modal" data-target="#importModal">
                            <i class="fa fa-upload" aria-hidden="true"></i> {{ "import_verb"|trans|capitalize }}
                        </button>
                    </li>
                    <li>
                        <a type="button" class="btn btn-warning btn-li" href="{{ path('other_customer_export_all') }}"><i class="fa fa-download" aria-hidden="true"></i> {{ "export_all"|trans|capitalize }}</a>
                    </li>
                    <li class="inherit-li">
                        <a class="btn btn-success btn-li" href="{{ path("other_customer_new") }}"><i class="fa fa-share-alt-square"></i> +{{ "addition"|trans|capitalize }}</a>
                    </li>
                    <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                    </li>
                    <li><a class="close-link"><i class="fa fa-close"></i></a>
                    </li>
                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content container">
                <a href="#" class="btn btn-default openall"><i class="fa fa-angle-down" aria-hidden="true"></i> {{ "unfold"|trans|capitalize }}</a>
                <a href="#" class="btn btn-default closeall"><i class="fa fa-angle-up" aria-hidden="true"></i> {{ "fold"|trans|capitalize }}</a>
                <hr>
                <!-- start accordion -->
                <div class="panel-group accordion" id="accordion" role="tablist" aria-multiselectable="true">
                    {% for otherCustomer in otherCustomers %}
                        <div class="panel">
                            <a class="panel-heading" role="tab" id="heading{{ otherCustomer.id }}" data-toggle="collapse" data-parent="#accordion" href="#collapse{{ otherCustomer.id }}" aria-expanded="true" aria-controls="collapse{{ otherCustomer.id }}">
                                <h4 class="panel-title">{{ otherCustomer.name }}</h4>
                            </a>
                            <div id="collapse{{ otherCustomer.id }}" class="panel-collapse collapse{% if loop.first %} in{% endif %}" role="tabpanel" aria-labelledby="heading{{ otherCustomer.id }}">
                                <div class="panel-body">
                                    <table class="table table-bordered">
                                        <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>{{ "name"|trans|capitalize }}</th>
                                            <th>{{ "phone_number"|trans|capitalize }}</th>
                                            <th>{{ "created_m_s"|trans|capitalize }} {{ "on_date"|trans }}</th>
                                            <th>{{ "updated_m_s"|trans|capitalize }} {{ "on_date"|trans }}</th>
                                            <th style="text-align: center;">{{ "actions"|trans|capitalize }}</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        <tr>
                                            <th scope="row">{{ otherCustomer.id }}</th>
                                            <td>{{ otherCustomer.name }}</td>
                                            <td>{{ otherCustomer.phoneNumber }}</td>
                                            <td>{% if otherCustomer.created %}{{ otherCustomer.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td>{% if otherCustomer.updated %}{{ otherCustomer.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
                                            <td align="center" style="vertical-align: middle">
                                                <a class="btn btn-sm btn-info" href="{{ path('other_customer_edit', { 'slug': otherCustomer.slug }) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i>  {{ "modify"|trans|capitalize }}</a>
                                                <a class="btn btn-sm btn-gold" href="{{ path('customer_contact_new', { 'slugCustomer': otherCustomer.slug }) }}"><i class="fa fa-user-plus" aria-hidden="true"></i>  {{ "add_contact"|trans|capitalize }}</a>
                                                <a class="btn btn-sm btn-warning" href="{{ path('other_customer_export', { 'slug': otherCustomer.slug }) }}"><i class="fa fa-download" aria-hidden="true"></i> {{ "export_verb"|trans|capitalize }}</a>
                                                <button id="import-btn" type="button" class="btn btn-sm btn-danger delete-entity" data-toggle="modal" data-target="#deleteModal" data-slug="{{ otherCustomer.slug }}">
                                                    <i class="fa fa-trash" aria-hidden="true"></i> {{ "delete_verb"|trans|capitalize }}
                                                </button>
                                        </tr>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
                <!-- end of accordion -->
            </div>
        </div>
    </div>

    <!-- Import Modal -->
    {% include  ":common:import_modal.html.twig" with { "action_path": "other_customer_index", "object_title": "some_other_customers", "error": error } %}
    <!-- Delete Modal -->
    {% include  ":common:remove_object_modal.html.twig" with { "default": true } %}

{% endblock %}

{% block custom_javascripts %}
    {{ parent() }}
    {% if error %}
        $(document).ready(function(){
        $("#import-btn").trigger("click");
        });
    {% endif %}
    {% include ":common:delete_pop_up.js.twig" with { "routeName": "other_customer_delete" } %}

{% endblock %}